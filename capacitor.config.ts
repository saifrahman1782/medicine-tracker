import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'final-year-project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
